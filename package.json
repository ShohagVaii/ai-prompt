{
  "name": "ai-prompt-generator",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/server.js",
    "build": "vessel build",
    "dev": "vessel dev",
    "predeploy": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "vessel": "^0.7.0"
  },
  "devDependencies": {
    "@vessel-js/react": "^0.7.0"
  }
}
